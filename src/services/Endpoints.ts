const Endpoints = {
     posts: "post",
     category: "category",
     auth: {
          login:'auth/login',
          logout:'auth/logout'
     },
     subscription: "subscription",
}
export default Endpoints;